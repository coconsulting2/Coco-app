/**
 * @module tests/frontend/setup
 * @description Vitest setup global — extiende expect con @testing-library/jest-dom
 * y configura globals comunes (MSW, etc.) si fuera necesario.
 */
import "@testing-library/jest-dom/vitest";
